# Task Index — model_training

One line per task file, newest first. Each entry: `id — outcome — slug — short hook`.

This index is a navigation aid for humans. Retrieval scripts read the task files directly, not this index, so don't worry if it drifts slightly.

---

<!-- Newest entries on top. Format:
- `YYYY-MM-DD-NNN` — outcome — short title — one-line hook
-->
